<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.findmydoc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindMyDoc">
        <activity android:name=".Awareness"></activity>
        <activity android:name=".Patient.PatientDashboard" />
        <activity android:name=".Doctor.DoctorDashboard" />
        <activity android:name=".UserTypeChooser" />
        <activity android:name=".Doctor.OTP_page" />
        <activity
            android:theme="@style/fullscreen"
            android:name=".Doctor.SignUpPage" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>